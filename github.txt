다른 Repositories를 바꾸고싶을때

git remote rm origin

git remote -v 

git init

git status

git remote add origin http~

git remote -v 

git add .

git status

git commit –m "first commit"

git push origin master

로 바꾸기